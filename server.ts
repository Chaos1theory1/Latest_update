// Legacy local Express server removed for Vercel deployment.
// The production backend lives in api/[...path].ts as a Vercel serverless function.
// Local frontend development uses: npm run dev
export {};
